import React, { FC } from "react";
import handicaps from '../handicaps.json';
import tables from '../mock-table-data.json';

const Row: any = () => {
    console.log(handicaps, tables);
    return null
};

export default Row;